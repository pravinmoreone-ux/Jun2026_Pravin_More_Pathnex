Day 04 — Basic CI/CD Concepts with Jenkins & GitLab

🔹 Jenkinsfile — Basic CI Pipeline pipeline { agent any stages { stage('Build') { steps { echo 'Building project' } } stage('Test') { steps { echo 'Running tests' } } stage('Deploy') { steps { echo 'Deploying application' } } } }
>>>>>>>>>>>>>>> Jenkins >>>>>>>>>>>>>>>>>>>>>>>>>>>>

pipeline {
    agent any

    stages {
        stage ('Build') {
            steps {
                echo 'Building Project'
            }
        }
        stage ('Test'){
            steps {
                echo 'Running tests'
            }
        }

        stage ('Deploy') {
            steps {
                echo 'Deploying application'               
            }            
        }
    }
}

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

gitlab

stages:
  - build
  - test
  - deploy

build:
  stage: build
  script: 
    echo "Building project" 

test
: stage: test
  script: 
    echo "test project" 

deploy:
  stage: deploy
  script: 
    echo "deploy project" 


=================================================


name: Basic CI/CD pipeline

on: 
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Build
        run: echo "Building Project"

      - name: Test
        run: echo "Test Project"

      - name: Deploy
        run: echo "Deploying application"
.

>>>>>>>>>>>>>>>>>>>>...


Ansible — Install and Configure Nginx

---
- name: Install and Configure Nginx
  hosts: all
  become: yes

  tasks:
    - name: Install nginx
      yum:
        name: nginx
        state: present

    - name: Config Nginx
      service:
        name: nginx
        state: started
        enabled: yes


>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

🔹 Terraform — EC2 with Security Group 
=========================
resource "aws_security_group" "example" { name = "example_security_group" description = "Allow SSH and HTTP" ingress { from_port = 22 to_port = 22 protocol = "tcp" cidr_blocks = ["0.0.0.0/0"] }

ingress { from_port = 80 to_port = 80 protocol = "tcp" cidr_blocks = ["0.0.0.0/0"] }

egress { from_port = 0 to_port = 0 protocol = "-1" cidr_blocks = ["0.0.0.0/0"] } }

============================================

resource "aws_security_group" "example" {
    name = "example_security_group"
    description = "Allow SSH and HTTP"

    ingress {
        from_port = 22
        to_port = 22
        protocol = "tcp"
        cidr_blocks = ["0.0.0.0/0"]
    }

    ingress {
        from_port = 80
        to_port = 80
        protocol = "tcp"
        cidr_blocks = ["0.0.0.0/0"]
    }

    egress {
        from_port = 0
        to_port = 0
        protocol = "-1"
        cidr_blocks = ["0.0.0.0/0"]
    }
}

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>


 Docker

Apache Container
FROM ubuntu:22.04 RUN apt update && apt install apache2 -y RUN 

echo "Hello Pathnex" > /var/www/html/index.html CMD ["apachectl", "-D", "FOREGROUND"]

Bash Real Path
/var/www/html

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

FROM ubuntu:22.04

RUN apt update && apt install apache2 -y

RUN echo "Hello Pravin" > /var/www/html/index.html 

CMD ["apachectl" , "-D" , "FOREGROUND"]



# Bash Real Path
/var/www/html

