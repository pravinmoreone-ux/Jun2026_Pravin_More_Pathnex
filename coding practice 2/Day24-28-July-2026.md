Day 24 — Advanced Terraform Modules and CI/CD Pipeline Scaling
🔹 Ansible — Deploy Docker Containers with Kubernetes Using Helm


---
- name: DEploy Docker container with helm
  hosts: all
  beome: yes
  tasks:
    - name: Install Helm
      command: curl curl https://raw.githubusercontent.com/helm/helm/master/scripts/get-helm-3 | bash
    - name: Deploy microservices with Helm
      command: helm install pravin-app ./microservice-chart

🔹 Terraform — Multi-Module Infrastructure with Outputs


module "pravin_vpc" {
  source = "./vpc"
}

module "pravin_instance" {
  source = "./instance"
  vpc_id = module.pathnex_vpc.vpc_id
}

output "instance_id" {
  value = module.pathnex_instance.instance_id
}


>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>


🔹 Kubernetes — Helm-based Auto-Scaling Deployments

apiVersion: apps/v1
kind: Deployment
metadata:
  name: pravin-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: helm-app
  template:
    metadata:
      labels:
        app: helm-app
    spec:
      containers:
        - name: nginx
          image: nginx
          ports:
           - containerPort: 80

---

apiVersion: autoscaling/v2
kind: HorizontalAutoscaler
metadata: 
  name: web-autoscaler
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: pravin-deployment
  minReplicas: 2
  maxReplicas: 10
# targetCPUUtilizationPercentage: 80
  
  metrics:
    - type: Resource
      resource: 
        name: cpu
        target: 
          type: Utilization
          averageUtilization: 80

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>..


🔹 Jenkinsfile — Scaling CI/CD Pipeline
pipeline {
    agent any
    stages {
        stage('Scale Up') {
            steps {
                echo 'Scaling up Jenkins pipeline agents for heavy workloads...'
                // Simulating scaling logic
            }
        }
        stage('Build') {
            steps {
                echo 'Building project...'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying project...'
            }
        }
    }
}


🔹 Docker
# Healthcheck

FROM nginx
HEALTHCHECK CMD curl --fail http://localhost || exit 1
 

