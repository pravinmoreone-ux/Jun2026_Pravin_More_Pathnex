
Day 29 — Final Project Setup and Configuration
🔹 Ansible — Complete Deployment Setup


---
- name: Complete Deployment Setup
  hosts: all
  become: yes
  tasks:
    - name: Deploy nginx , mysql, redis with docker
      command: docker-compose -f /path/to/decoker-compose.yaml up -d


>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

🔹 Terraform — Provisioning Multi-Tier Application with VPC and Security

resource "aws_vpc" "pravin_vpc" {
    cidr_block = "10.0.0.0/16"
}

resource "aws_instance" "pravin_instance" {
    ami           = "ami-0abcd1234abcd1234"
    instance_type = "t2.medium"
    subnet_id     = aws_subnet.pathnex_subnet.id
    security_groups = [aws_security_group.pravin_sg.name]
}


>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>...




🔹 Kubernetes — Deploy Full Application Stack

apiVersion: apps/v1
kind: Depoyment
metadata: 
  name: pravin-app
spec: 
  replicas: 3
  selector:
    matchLabels:
      app: pravin-app
  template:
    metadata:
      labels:
        app: pravin-app
    spec:
      containers:
        - name: nginx
          image: nginx
          ports:
            - containerPort: 80

---

apiVersion: v1
kind: Service
metadata: 
  name: pravin-service
spec:
  selector:
    app: pravin-app
  ports:
    - port: 80
      targetPort: 80

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>.

Docker 



FROM nginx
RUN useradd pravinuser
USER pravinuser

docker container prune
docker image prune 
docker volume prune 
docker network prune 
docker system prune -a

#Final Docker Projectg Pravin

version: '3'
services:
  frontend:
    image: nginx
    container_name: pravin-frontend
    ports: 
      - "80:80"
  backend:
    build: ./backend
    container-name: pravin-backend
    ports: 
      - "5000:5000"
  database:
    image: mysql:8
    container_name: pravin-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root







