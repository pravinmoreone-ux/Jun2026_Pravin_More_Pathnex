Day 19 — Advanced Monitoring, Logging, and Security
🔹 Ansible — Setup Prometheus for Monitoring


---
- name:  Install Prometheus for Monitoring
  hosts: all
  become: yes
  tasks:
    - name: Install Prometheus dependencies
      yum: 
        name: 
          - promethues
          - prometheus-node-exporter
        state: present

    - name: start prometheus service
      service: 
        name: prometheus
        state: started
        enabled: yes
    
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>..

Terraform — Setup AWS CloudWatch for Monitoring

resource "aws_cloudwatch_log_group" "pravin_log_group" {
    name = "pravin_log_group"
}

resource "aws_cloudwatch_log_stream" "pravin_log_stream" {
  log_group_name = aws_cloudwatch_log_group.pravin_log_group.name
  name           = "pravin_log_stream"
}


>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>.



🔹 Kubernetes — Deploy Fluentd for Logging

apiVersion: apps/v1
kind: Deployment
metadata: 
  name: pravindeployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fluentd
  template:
    metadata:
      labels:
        app: fluentd
    spec:
      containers:
        - name: fluentd
          image: fluentd
          port:
            - containerPort: 24224

---

apiVersion: v1
kind: Service
metadata:
  name: fluentds
spec:
  selector:
    app: fluentd
  ports:
    - protocol: TCP
      port: 24224

>>>>>>>>>>>>>>>>


🔹 Jenkinsfile — Integrating Prometheus Monitoring in Pipeline
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building Docker image...'
                docker.build('pathnex-web-app')
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests...'
            }
        }
        stage('Monitor Deployment') {
            steps {
                script {
                    sh 'kubectl apply -f kubernetes/deployment.yaml'
                    sh 'kubectl get pods --watch'
                }
            }
        }
    }
}
🔹 GitLab CI/CD — Prometheus and Logging Integration
stages:
  - build
  - push
  - deploy

build:
  stage: build
  script:
    - docker build -t pathnex-web-app .

push:
  stage: push
  script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD"
    - docker push pathnex-web-app

deploy:
  stage: deploy
  script:
    - kubectl apply -f kubernetes/deployment.yaml
    - kubectl get pods --watch



🔹 Docker
# Flask Application (Python)
from flask import Flask
app = Flask(__name__)
@app.route('/')
def home():
    return "Hello Pathnex"
app.run(host='0.0.0.0', port=5000)

# Docker File
FROM python:3.11
WORKDIR /opt/pathnex/flask-app
COPY . /opt/pathnex/flask-app/
RUN pip install flask
CMD ["python", "/opt/pathnex/flask-app/app.py"]
>>>>>>>>>>>>>>>>>>>>>>>>.
from flask import Flask
@app.route('/')
def home():
    return "Hello Pravin"
app.run(host='0.0.0.0', port=5000)

#Docker File
FROM python:3.11
WORKDIR /opt/pravin/flask-app
COPY . /opt/pravin/flask-app
RUN pip install flask
CMD ["python", "/opt/pathnex/flask-app/app.py"]
