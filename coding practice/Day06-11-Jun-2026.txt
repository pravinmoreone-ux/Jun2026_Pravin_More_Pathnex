# Day 06 — DEBUG DAY

Fix these broken files:????

## PRAVIN MORE fixing Ansible (broken)

- name: Install nginx
  hosts: all
  become: yes
    
  tasks:
    - name: Install nginx
      yum:
        name: nginx
        state: present

## colon was not presnt in between key and value and indentation was incorrect

///        

## 🔹 PM Terraform (broken)

resource "aws_instance" "BrokenEC2" {
  ami           = "ami-0xyz"
  instance_type = "t2.micro"  ## instance type value not in quotes
  ## indentation was incorrect
  tags = {
    Name = "Broken"
    }
}



## PM CHECKING Kubernetes (broken)
##colon issue
apiVersion: v1
kind: Pod
metadata:
  name: pathnex ## colon between key and value and indentation problem
spec:  ## colon not present in front od spec
  containers: ## indentation problem
    - name: app
      image: nginx

####################



#  Debugging Pipelines
## 🔹 Jenkins Pipeline — Debugging
You will learn how to **fix broken stages and handle errors**.

pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/Pathnex/sample-java-app.git'
            }
        }
        stage('Build') {
            steps {
                sh '''
                mvn clean compile || { echo "Compile failed"; exit 1; }
                '''
            }
        }
        stage('Test') {
            steps {
                sh '''
                mvn test || { echo "Tests failed"; exit 1; }
                '''
            }
        }
    }
}




#########################3333


## 🔹 GitLab CI — Debugging
You will learn how to **handle job failures and exit codes**.

## Defines the order of pipeline stages.
stages:
  - build
  - test

##If the build stage fails, the test stage will not run.
build: ### Creates a job named build.
  stage: build
  image: maven:3.8.1-jdk-17
  script:
    - git clone https://github.com/Pathnex/sample-java-app.git
    - cd sample-java-app
    - mvn clean compile || { echo "Compile failed"; exit 1; }

test: ### create a job test
  stage: test
  image: maven:3.8.1-jdk-17
  script:
    - cd sample-java-app
    - mvn test || { echo "Tests failed"; exit 1; }


    ##vArtifacts are files or directories generated by a job and stored by GitLab
     so they can be downloaded or used by subsequent jobs and stages in the pipeline.

    Q: Why can the test job fail even if the build job succeeds?

Answer:
Each GitLab CI job runs in a separate container/environment.
 Files created in one job are not automatically
  available in the next job unless they are passed using artifacts 
  or recreated in the new job.