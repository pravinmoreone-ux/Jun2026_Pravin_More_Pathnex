# Day 10 — Conditions, Route Table, Secret, Loops

## 🔹 Ansible — When Condition

Ansible `when` condition is used to run a task only if a condition is true.

### Example 1: Install Apache only on Ubuntu

---
- name: Install Apache on Ubuntu
  hosts: all
  become: yes
  
  tasks:
    - name: Install htop
      yum:
        name: htop  
        state: present
      when: ansible_facts['os_family'] == "RedHat"


### ##

Terraform - Route Table + Association

resource "aws_route_table" "PravinRT" {
  vpc_id = aws_vpc.PravinVPC.id

  route {
    cidr_block  = "0.0.0.0/0"
    gateway_id = aws_internet_gateway.PravinIGW.id
  }

  tags = {
    Name = "Pathnex-RouteTable"
  }
}

resource "aws_route_table_association" "PathnexRTA" {
  subnet_id      = aws_subnet.PathnexSubnet.id
  route_table_id = aws_route_table.PathnexRT.id
}




### Kubernetes Secrets

apiVersion: v1
kind: Secret
metadata:
  name: pravin-secrets
type: Opaque
data:
  example: "<BASE64_VALUE>"


### SHell Script for loop

#!/bin/bash

for i in {1..10}; do
  echo "Number : $i "
done


###  Jenkins - Cache maven dependencies


pipeline {
  agent any
  environment {
    INSTITUE = "Pravin"
  }

  Stages {
    Stage('Checkout') {
      Steps {
        git url: 'https://github.com/pravin/sample-java-app.git'
      }
    }

    stage('Build') {
            steps {
                sh '''
                if [ -d ".m2" ]; then
                    echo "Using cached dependencies"
                else
                    mvn clean compile
                fi
                '''
            }
        }
  }
}


##  






## 🔹 GitLab CI — Cache Dependencies
You will learn how to **cache dependencies in GitLab CI**.

stages:
  - build

build:
  stage: build
  image: maven:3.8.1-jdk-17
  cache:
    key: maven-cache
    paths:
      - .m2/repository
  script:
    - git clone https://github.com/Pathnex/sample-java-app.git
    - cd sample-java-app
    - mvn clean compile









